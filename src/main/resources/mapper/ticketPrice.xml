<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="edu.du.airline_project.repository.interfaces.TicketPriceRepository">

<select id="selectByHours" resultType="edu.du.airline_project.repository.model.TicketPrice">
	SELECT * FROM TICKET_PRICE_TB WHERE flight_hours = #{flightHours};
</select>

</mapper>